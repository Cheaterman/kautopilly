#:import Connection views.Connection
#:import Atmospheric views.Atmospheric

ScreenManager:
    Connection:
        id: connection
        on_connect:
            app.connect(
            self.address,
            self.rpc_port,
            self.stream_port,
            )

    Atmospheric:
        id: atmospheric
